import styled from 'styled-components';

const Title = styled.div`
  width: '100%';
  color: #000000;
  font-size: 40px;
  font-weight: bold;
  margin-bottom: 25px;
  padding-left: 20px;
`;

export default Title;
